<!DOCTYPE html>
<html lang="en">

<head>
  <% include partials/_scripts %>

</head>

<body>
  <% include partials/_header %>
  <h1>Admin Page</h1>
  <h1 class="polltitle"><%= Object.values(options[0])[1]%> </h1>
  <h3>Results here:</h3>
  <ol>
    <% for (let i = 0; i < options.length; i++) {%>
    <li>
      <%= Object.values(options[i])[0] %>
    </li>
    <% };%>
</ol>

<h5>This is the voting <a href="/<%=randomURL%>">link</a></h5>
</body>

</html>
